// 27.07.26

let nota = 75;
let presenca = 80;
let aprovado;

if (nota >= 60 && presenca >= 75) {
    aprovado = true;
    console.log("Aprovado: " + aprovado);
} else {
    aprovado = false;
    console.log("Aprovado: " + aprovado);
}